from setuptools import setup, find_packages

setup(
    name='Aduplicate',
    version='0.2',
    packages=find_packages(),
    install_requires=[
        'kafka-python-ng>2.0.0',
        ''
    ],
)
